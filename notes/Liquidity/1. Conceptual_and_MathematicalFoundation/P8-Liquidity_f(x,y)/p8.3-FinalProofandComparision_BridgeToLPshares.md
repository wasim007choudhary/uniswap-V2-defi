# 8. What Is Liquidity? (F(x, y))

# Part 3 — Final Proof, Comparison, and Bridge to LP Shares

---

# 📌 One Last Question

By the end of Part 2 we concluded:

```text
2x

✅ Valid

↓

2y

✅ Valid

↓

√(xy)

✅ Valid and Chosen
```

At this point another question naturally appeared.

---

# 💬 Our Discussion

You asked:

> **"Didn't only `√(xy)` get selected? Why are we still proving things for `2x` and `2y`?"**

This is an important distinction.

---

## Clarification

There are two completely different questions.

### Question 1

> **Is this a mathematically valid liquidity function?**

Answer:

```text
2x

✅ Yes
```

```text
2y

✅ Yes
```

```text
√(xy)

✅ Yes
```

---

### Question 2

> **Which one did Uniswap choose?**

Answer:

```text
√(xy)
```

Not because:

```text
2x
```

is wrong.

Not because:

```text
2y
```

is wrong.

But because:

```text
√(xy)
```

has additional desirable properties.

Therefore we still prove all three.

---

# 📌 Why Are We Doing Another Proof?

I then introduced:

```text
(L₁ − L₀)/L₀

=

dx/x

=

dy/y
```

---

# 💬 Your Question

You immediately asked:

> **"Why are you giving me this?"**

Great question.

The answer is:

Because this is the bridge between:

```text
Liquidity
```

and

```text
LP Shares
```

Remember our LP Share Formula?

```text
S

=

((L₁ − L₀)/L₀)

×

T
```

Notice something.

It contains:

```text
(L₁ − L₀)/L₀
```

Until now...

We never actually proved what that quantity equals.

This proof completes that missing piece.

---

# Candidate 1 — L = 2x

Before adding liquidity:

```text
L₀

=

2x
```

After adding liquidity:

```text
L₁

=

2(x + dx)
```

Compute:

```text
(L₁ − L₀)/L₀
```

Substitute.

```text
(2(x + dx) − 2x)

/

2x
```

Expand.

```text
(2x + 2dx − 2x)

/

2x
```

Cancel.

```text
2dx

/

2x
```

Simplify.

```text
dx/x
```

Therefore:

```text
(L₁ − L₀)/L₀

=

dx/x
```

---

# Interpretation

If liquidity is measured entirely in Token X...

Then the percentage increase in liquidity equals the percentage increase of Token X.

Exactly what we'd expect.

---

# Candidate 2 — L = 2y

Now suppose:

```text
L₀

=

2y
```

After adding liquidity:

```text
L₁

=

2(y + dy)
```

Again compute:

```text
(L₁ − L₀)/L₀
```

Substitute.

```text
(2(y + dy) − 2y)

/

2y
```

Expand.

```text
2y + 2dy − 2y
```

Cancel.

```text
2dy

/

2y
```

Simplify.

```text
dy/y
```

Therefore:

```text
(L₁ − L₀)/L₀

=

dy/y
```

---

# Before The Final Proof

I then pointed out something interesting.

For:

```text
2x
```

we obtained:

```text
dx/x
```

For:

```text
2y
```

we obtained:

```text
dy/y
```

But in the previous chapter we already proved:

```text
dy/dx

=

y/x
```

Rearranging gives:

```text
dx/x

=

dy/y
```

This means both previous proofs are already saying the same thing.

---

# Candidate 3 — L = √(xy)

Before adding liquidity:

```text
L₀

=

√(xy)
```

After adding liquidity:

```text
L₁

=

√((x + dx)(y + dy))
```

From the previous chapter:

```text
dy

=

(y/x)

×

dx
```

Substitute into the equation.

```text
L₁

=

√((x + dx)

×

(y + (y/x)dx))
```

Factor each bracket.

```text
x + dx

=

x(1 + dx/x)
```

```text
y + (y/x)dx

=

y(1 + dx/x)
```

Substitute.

```text
L₁

=

√(xy

×

(1 + dx/x)²)
```

Take the square root.

```text
L₁

=

√(xy)

×

(1 + dx/x)
```

Since:

```text
√(xy)

=

L₀
```

we obtain:

```text
L₁

=

L₀

×

(1 + dx/x)
```

Subtract:

```text
L₀
```

from both sides.

```text
L₁ − L₀

=

L₀(dx/x)
```

Divide by:

```text
L₀
```

Giving:

```text
(L₁ − L₀)/L₀

=

dx/x
```

And because:

```text
dx/x

=

dy/y
```

Finally:

```text
(L₁ − L₀)/L₀

=

dx/x

=

dy/y
```

---

# 🎉 Final Result

All three valid liquidity definitions produce the same proportional liquidity growth.

For:

```text
2x
```

```text
(L₁ − L₀)/L₀

=

dx/x
```

For:

```text
2y
```

```text
(L₁ − L₀)/L₀

=

dy/y
```

For:

```text
√(xy)
```

```text
(L₁ − L₀)/L₀

=

dx/x

=

dy/y
```

---

# 🧠 Huge Realization

This surprised us.

Even though the liquidity functions look completely different...

They all predict exactly the same percentage increase in liquidity.

Therefore...

The LP Share Formula does **not** depend on choosing:

```text
2x
```

or

```text
2y
```

or

```text
√(xy)
```

It only depends on the percentage increase in liquidity.

---

# Final Comparison

| Liquidity Function | Valid? | Reason |
|--------------------|---------|--------|
| `x + y` | ❌ | Different asset units cannot be added directly. |
| `2x` | ✅ | Measures the entire pool in Token X units. |
| `2y` | ✅ | Measures the entire pool in Token Y units. |
| `xy` | ❌ | Measures liquidity², causing quadratic scaling. |
| `√(xy)` | ✅ **Chosen** | Symmetric, linear, naturally derived from the constant product invariant, and independent of token ordering. |

---

# Common Misconceptions

### ❌ Only `√(xy)` is mathematically correct.

No.

`2x` and `2y` are also mathematically valid liquidity functions.

Uniswap simply chose the most elegant one.

---

### ❌ `xy` fails because it isn't symmetric.

No.

It is perfectly symmetric.

It fails because it measures **Liquidity²**, not liquidity.

---

### ❌ We randomly decided to use a square root.

No.

The square root appears naturally because:

```text
xy

=

Liquidity²
```

Therefore:

```text
Liquidity

=

√(xy)
```

---

### ❌ The LP Share Formula only works because of `√(xy)`.

No.

The LP Share Formula depends on **proportional liquidity growth**, which all valid liquidity functions satisfy.

---

# 🧠 Biggest Realizations

- We first identified the properties a good liquidity function should have.
- We evaluated multiple candidates instead of guessing one.
- `2x` and `2y` are mathematically correct.
- `xy` is symmetric but measures liquidity squared.
- `√(xy)` is linear, symmetric, and naturally derived from the constant product invariant.
- All valid liquidity functions produce the same proportional liquidity growth.
- Uniswap chose `√(xy)` because it is the cleanest and most natural definition.

---

# 🔗 Bridge To The Next Section

We can now return to the LP Share Formula with complete understanding.

We already derived:

```text
S

=

((L₁ − L₀)/L₀)

×

T
```

Now we know exactly what every symbol means.

```text
L₀

=

√(x₀y₀)
```

```text
L₁

=

√(x₁y₁)
```

The remaining task is simply to substitute these expressions into the LP Share Formula and derive the exact equation implemented inside Uniswap V2's `Pair.mint()` function.

At this point, the mathematics behind liquidity is complete. The next chapter transforms this mathematical understanding into the final LP minting equation used by the protocol.